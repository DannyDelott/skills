interface:
  display_name: "Publish Changes"
  short_description: "Commit, push, and open a ready-for-review PR"
  default_prompt: "Use $yeet to confirm the intended changes, create a focused commit, push it, and open a ready-for-review pull request. Do not create a draft pull request unless the user explicitly asks for draft."
