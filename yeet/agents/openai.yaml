interface:
  display_name: "Publish Changes"
  short_description: "Commit, push, and open a ready-for-review PR"
  default_prompt: "Use $yeet to confirm the intended changes, create a focused commit, push it, and open a ready-for-review pull request. Use draft status only when the user explicitly asks for it."
